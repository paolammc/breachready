import type { ConsoleExercise } from '../../types';

export const consoleExercises: ConsoleExercise[] = [
  // Beginner Exercises
  {
    id: 'EX-001',
    scenarioText: 'You need to identify all listening TCP and UDP ports on a Linux server to check for unauthorized services.',
    expectedCommands: ['ss -tulpn', 'ss -tulnp', 'netstat -tulpn', 'netstat -tulnp'],
    hints: ['Use the ss command with flags for TCP, UDP, listening, and process info', 'The flags are: -t (TCP), -u (UDP), -l (listening), -p (process), -n (numeric)'],
    explanation: 'The ss command (socket statistics) is the modern replacement for netstat. The flags -tulpn show: -t (TCP), -u (UDP), -l (listening ports only), -p (process using the port), -n (numeric, don\'t resolve names). This reveals all services accepting connections.',
    difficulty: 'Beginner',
  },
  {
    id: 'EX-002',
    scenarioText: 'Run a SYN stealth scan on target 192.168.1.50 and detect what service versions are running on open ports.',
    expectedCommands: ['nmap -sS -sV 192.168.1.50', 'nmap -sV -sS 192.168.1.50'],
    hints: ['Combine SYN scan (-sS) with version detection (-sV)', 'nmap flags can be in any order'],
    explanation: 'A SYN stealth scan (-sS) sends SYN packets but never completes the TCP handshake, making it harder to detect. Adding -sV probes open ports to determine service versions. This combination is stealthy yet informative.',
    difficulty: 'Beginner',
  },
  {
    id: 'EX-003',
    scenarioText: 'Scan ALL 65535 ports on target 192.168.1.50, skipping host discovery (the target blocks ping).',
    expectedCommands: ['nmap -p- -Pn 192.168.1.50', 'nmap -Pn -p- 192.168.1.50'],
    hints: ['Use -p- to scan all ports and -Pn to skip ping', 'The target IP goes at the end'],
    explanation: 'The -p- flag tells nmap to scan all 65535 TCP ports instead of just the top 1000. The -Pn flag skips host discovery (ping), useful when ICMP is blocked. This thorough scan catches services on non-standard ports.',
    difficulty: 'Intermediate',
  },
  {
    id: 'EX-004',
    scenarioText: 'Capture all network traffic on interface eth0 and save it to a file called capture.pcap for later analysis.',
    expectedCommands: ['tcpdump -i eth0 -w capture.pcap'],
    hints: ['Use tcpdump with -i for interface and -w for write', 'The output filename should be capture.pcap'],
    explanation: 'tcpdump is a command-line packet analyzer. The -i flag specifies the network interface to capture from, and -w writes the raw packets to a file in pcap format. This file can be analyzed later with Wireshark or other tools.',
    difficulty: 'Intermediate',
  },
  {
    id: 'EX-005',
    scenarioText: 'Count how many failed SSH login attempts are recorded in /var/log/auth.log.',
    expectedCommands: ['grep "Failed password" /var/log/auth.log | wc -l', "grep 'Failed password' /var/log/auth.log | wc -l"],
    hints: ['Use grep to find "Failed password" entries, then pipe to wc -l', 'The pattern is case-sensitive'],
    explanation: 'This command chain searches for "Failed password" in the authentication log (indicating failed SSH attempts) and pipes the results to wc -l which counts the lines. High counts may indicate a brute-force attack.',
    difficulty: 'Intermediate',
  },
  {
    id: 'EX-006',
    scenarioText: 'Generate a new 4096-bit RSA SSH key pair for secure authentication.',
    expectedCommands: ['ssh-keygen -t rsa -b 4096', 'ssh-keygen -b 4096 -t rsa'],
    hints: ['Use ssh-keygen with -t for type and -b for bits', '4096 bits provides strong security'],
    explanation: 'ssh-keygen generates SSH key pairs. The -t rsa specifies the RSA algorithm, and -b 4096 sets the key length to 4096 bits. Longer keys are more secure but slightly slower. The private key stays on your machine; the public key goes to servers.',
    difficulty: 'Intermediate',
  },
  {
    id: 'EX-007',
    scenarioText: 'Create a self-signed TLS certificate valid for 365 days using an existing CSR (server.csr) and private key (server.key). Output to server.crt.',
    expectedCommands: ['openssl x509 -req -days 365 -in server.csr -signkey server.key -out server.crt'],
    hints: ['Use openssl x509 with -req flag for CSR input', 'Specify -days, -in (CSR), -signkey (private key), and -out (certificate)'],
    explanation: 'This openssl command creates a self-signed certificate from a Certificate Signing Request. The -req flag indicates CSR input, -days 365 sets validity, -signkey uses your private key to sign (self-signed), and -out specifies the output certificate file.',
    difficulty: 'Advanced',
  },
  {
    id: 'EX-008',
    scenarioText: 'Use PowerShell to display the last 20 entries from the Windows Security event log.',
    expectedCommands: ['Get-EventLog -LogName Security -Newest 20'],
    hints: ['Use Get-EventLog cmdlet with -LogName and -Newest parameters', 'PowerShell cmdlets use PascalCase'],
    explanation: 'Get-EventLog is a PowerShell cmdlet for reading Windows event logs. The -LogName parameter specifies which log (Security contains auth events), and -Newest 20 returns only the most recent 20 entries. Essential for incident investigation.',
    difficulty: 'Intermediate',
  },
  {
    id: 'EX-009',
    scenarioText: 'Block all inbound traffic from attacker IP 10.10.10.5 using iptables on Linux.',
    expectedCommands: ['iptables -A INPUT -s 10.10.10.5 -j DROP'],
    hints: ['Use iptables -A INPUT to append to the INPUT chain', 'Use -s for source IP and -j DROP to block'],
    explanation: 'iptables is the Linux firewall. -A INPUT appends a rule to the INPUT chain (incoming traffic). -s specifies the source IP to match, and -j DROP silently discards matching packets. This immediately blocks all traffic from the attacker.',
    difficulty: 'Intermediate',
  },
  {
    id: 'EX-010',
    scenarioText: 'Enable the UFW firewall and configure it to allow only HTTPS (port 443) inbound traffic.',
    expectedCommands: ['ufw enable && ufw allow 443/tcp', 'ufw enable && ufw allow 443'],
    hints: ['First enable UFW, then allow port 443', 'Use && to chain the commands'],
    explanation: 'UFW (Uncomplicated Firewall) is a user-friendly frontend for iptables. "ufw enable" activates the firewall, and "ufw allow 443/tcp" permits inbound HTTPS traffic. The && ensures the second command only runs if the first succeeds.',
    difficulty: 'Beginner',
  },
  {
    id: 'EX-011',
    scenarioText: 'Find all files with the SUID bit set on the entire filesystem. These could be privilege escalation vectors.',
    expectedCommands: ['find / -perm -4000', 'find / -perm -4000 2>/dev/null', 'find / -perm -u=s', 'find / -perm -u=s 2>/dev/null'],
    hints: ['Use find with -perm flag to search by permissions', 'SUID is represented as 4000 in octal or u=s symbolically'],
    explanation: 'Files with SUID bit run with the owner\'s privileges (often root). find / searches from root, and -perm -4000 matches files with SUID set. Attackers look for vulnerable SUID binaries for privilege escalation. 2>/dev/null hides permission errors.',
    difficulty: 'Advanced',
  },
  {
    id: 'EX-012',
    scenarioText: 'Identify which process is currently using port 4444 (a common Metasploit port).',
    expectedCommands: ['lsof -i :4444', 'lsof -i:4444'],
    hints: ['Use lsof (list open files) with -i for network connections', 'Specify the port with :4444'],
    explanation: 'lsof lists open files, including network sockets. The -i flag filters for internet connections, and :4444 specifies the port. This reveals the process name and PID using that port. Port 4444 is Metasploit\'s default - finding it is a red flag!',
    difficulty: 'Intermediate',
  },
  {
    id: 'EX-013',
    scenarioText: 'Calculate the SHA-256 hash of a forensic disk image file named evidence.img to verify integrity.',
    expectedCommands: ['sha256sum evidence.img'],
    hints: ['Use sha256sum followed by the filename', 'This creates a cryptographic hash for integrity verification'],
    explanation: 'sha256sum computes a SHA-256 cryptographic hash of the file. In forensics, you hash evidence immediately upon collection and compare later to prove the evidence wasn\'t modified. SHA-256 is collision-resistant and widely accepted in court.',
    difficulty: 'Beginner',
  },
  {
    id: 'EX-014',
    scenarioText: 'Display all Group Policies currently applied to the current user and computer on Windows.',
    expectedCommands: ['gpresult /r', 'gpresult /R'],
    hints: ['Use gpresult with the /r flag for a summary report', 'This shows both user and computer policies'],
    explanation: 'gpresult displays the Resultant Set of Policy (RSoP) - all Group Policies currently applied. The /r flag generates a summary report showing which GPOs are applied, security groups, and any denied GPOs. Essential for troubleshooting policy issues.',
    difficulty: 'Intermediate',
  },
  {
    id: 'EX-015',
    scenarioText: 'Add the user "analyst" to the sudo group on Linux, granting them administrative privileges.',
    expectedCommands: ['usermod -aG sudo analyst'],
    hints: ['Use usermod with -aG to append to a group', 'The -a flag is critical - without it, the user loses other group memberships'],
    explanation: 'usermod modifies user accounts. The -aG flags mean "append to group" - critically, -a prevents removing existing group memberships. Adding a user to the sudo group grants them the ability to run commands as root using sudo.',
    difficulty: 'Beginner',
  },
];
